apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb
  labels:
    app.kubernetes.io/name: mongodb
spec:
  serviceName: mongodb
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: mongodb
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mongodb
    spec:
      serviceAccountName: mongodb
      securityContext:
        # Keep /data/db writable.
        fsGroup: 999
        sysctls: []
      containers:
        - name: mongodb
          image: mongo:4.4.28-focal
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
          ports:
            - name: mongodb
              containerPort: 27017
          # Probe MongoDB without authentication.
          livenessProbe:
            exec:
              command:
                - bash
                - -ec
                - |
                  mongo --norc --quiet --eval "db.adminCommand('ping')"
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 6
          readinessProbe:
            exec:
              command:
                - bash
                - -ec
                - |
                  mongo --norc --quiet --eval "db.adminCommand('ping')"
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 6
          resources:
            limits: {}
            requests: {}
          volumeMounts:
            - name: datadir
              mountPath: /data/db
      # Set PV ownership.
      initContainers:
        - name: fix-perms
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 999:999 /data/db || true"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: datadir
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: datadir
      spec:
        # Match the static PV storage class.
        storageClassName: longhorn
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: "1Gi"
